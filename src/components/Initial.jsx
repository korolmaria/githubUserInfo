import React from 'react';

const Initial = () => {
    return (
        <section class="repositories repositories_initial">
            <div class="container">
            </div>
        </section>
    );
}

export default Initial;