import React from 'react';

const UserNotFound = () => {
    return (
        <section class="repositories repositories_user-not-found">
            <div class="container">

            </div>
        </section>
    );
}

export default UserNotFound;